# Assertion consumer service (ACS)

## Description
The assertion consumer service (ACS) is the URL at the relying party (RP) where the SAML assertion is sent. The ACS is the endpoint that the identity provider (IdP) sends the SAML assertion to. The ACS is typically a URL at the RP that is expecting the SAML assertion. The ACS is the endpoint that the IdP sends the SAML assertion to. The ACS is typically a URL at the RP that is expecting the SAML assertion.
